version: '2'

options:
  verbose: true
syncs:
  sync-test:
    src: '/data/www/test.com'
    #sync_strategy: 'unison' #not needed, this is the default now
    # 同步时的用户ID，不指定的话为root
    sync_userid: '1000'
    sync_excludes: ['.git','.ieda']
  sync-test2:
    src: '/data/www/test2.com'
    #sync_strategy: 'unison' #not needed, this is the default now
    # 同步时的用户ID，不指定的话为root
    sync_userid: '1000'
    sync_excludes: ['.git','.ieda']
